import { ReturnCase } from '../types';

/**
 * Lista inicial de devoluções. Fica vazia de propósito: o sistema começa
 * "zerado" e cada devolução real é cadastrada pelo botão "Nova Devolução".
 */
export const INITIAL_RETURNS: ReturnCase[] = [];
